<div data-controller="list">
  <div class="list-buttons columns">
    <div class="column">
      <%= link_to new_product_category_path, class: 'button is-primary' do %>
        Novo
      <% end %>
    </div>
    <div class="column mass-actions" data-list-target="massActionsContainer">
      <button data-action="click->list#destroySelected" data-destroy-url="<%= product_category_url(id: ':id:') %>.json" class="button is-danger">
        Excluir
      </button>
    </div>
  </div>

  <table class="table is-striped is-narrow is-hoverable is-fullwidth">
    <thead>
      <tr>
        <th>&nbsp;</th>
              <th>Name</th>
            </tr>
    </thead>
    <tbody>
    <% @product_categories.each do |product_category| %>
      <tr data-list-target="items" data-edit-url="<%= edit_product_category_url(product_category) %>">
        <td class="item-check">
          <input type="checkbox" value="<%= product_category.id %>" data-action="change->list#itemSelect">
        </td>
              <td class="clickable" data-action="click->list#edit">
      <%= product_category.name %>
        </td>
              </tr>
    <% end %>
    </tbody>
  </table>
<div>

<%= will_paginate @product_categories %>
